class Solution:
  def arraySign(self, nums: List[int]) -> int:
          if nums.count(0) >=1:
              return 0
          neg = sum([1 for i in nums if i <0])
        
          if neg %2 == 0:
              return 1
          else:
              return -1
